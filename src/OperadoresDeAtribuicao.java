public class OperadoresDeAtribuicao {
    //Operadores de Atribuição
    public static void main(String[] args){
       /*
       Todos operadores de atribuição são utilizados com o sinal de = que são:
        =, -=, +=, *=, /=, %=
        são feitos para poupar linha de cófigos
        */

       //int salario = 1800;
        //salario = salario + 1000;//normal
        //salario += 1000;//abreviado
        //salario -= 1000;//abreviado
        //salario *=  0.1;//abreviado ou 0.1 ver porcentagem
        //salario = salario + (int) (salario * 0.1);//abreviado acrescentar
        //System.out.println(salario);


        int numero = 11;
        numero %= 2;

        System.out.println(numero);
    }
}
